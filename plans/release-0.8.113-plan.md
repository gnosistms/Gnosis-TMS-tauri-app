# Release 0.8.113

## Plan

1. Bump the release metadata from 0.8.112 to 0.8.113.
2. Add release notes for the editor sidebar and selection improvements merged in PR #326.
3. Run release-relevant validation, commit the release metadata on `main`, push it, and tag that exact commit as `v0.8.113`.

## Validation

Pending.
