# Release 0.8.114

## Plan

1. Bump the release metadata from 0.8.113 to 0.8.114.
2. Add release notes for the editor sidebar offscreen-row notice restyle merged in PR #327.
3. Run release-relevant validation, commit the release metadata on `main`, push it, and tag that exact commit as `v0.8.114`.

## Validation

- `npm test` — 23/23 passing.
