# Empty footnote reopen fix

1. Make the pointerdown guard recognize the full closed-footnote row as an editor control.
2. Add regression coverage for the empty marker target and image-upload dismissal guard.
3. Run focused and full frontend verification before publishing.
